import preloader from './preloader.gif';

const Preloader = () => {
    return (
        <div style={{textAlign: "center"}}>
            <img src={preloader}/>
        </div>
    )
}

export default Preloader;